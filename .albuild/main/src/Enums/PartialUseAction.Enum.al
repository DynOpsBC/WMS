enum 72204 "DOPSWHS Partial Use Action"
{
    Extensible = true;

    value(0; CreateNewLP) { Caption = 'Create New LP'; }
    value(1; RemoveExcess) { Caption = 'Remove Excess'; }
    value(2; RemoveUsedPortion) { Caption = 'Remove Used Portion'; }
    value(3; Unbuild) { Caption = 'Unbuild'; }
}
