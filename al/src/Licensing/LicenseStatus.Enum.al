enum 72211 "DOPSWHS License Status"
{
    Extensible = true;

    value(0; Unknown) { Caption = 'Unknown'; }
    value(1; Active) { Caption = 'Active'; }
    value(2; Expired) { Caption = 'Expired'; }
    value(3; Invalid) { Caption = 'Invalid'; }
    value(4; Offline) { Caption = 'Offline (grace)'; }
    value(5; Revoked) { Caption = 'Revoked'; }
}
